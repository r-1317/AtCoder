# 生成AIに与えるテンプレート集

## 問題文の書き起こし
以下のhtmlに示す問題がある。
```
<問題ページのhtml>
```
このhtmlの問題のタイトルから提出欄の直前までの部分を、1字1句違わずに書き起こしてほしい。

## PythonからC++への変換
以下に示す問題がある。
```
<書き起こされた問題文>
```
この問題に対する解として、私はPython3で以下のコードを作成した。
```Python
<Pythonで書いたコード>
```
このコードを、C++に変換してほしい。

### プロンプトのメモ


#### 03
02から

今のプログラムだと近くにあるスイッチをまとめて押せてしまうため、足止めとしての効果が薄くなっています。スイッチ同士の間に壁を設け、遠くにあるスイッチの集まり同士を交互に行き来させるように改良してください。また、スイッチまでの経路を塞ごうとしても横から入れてしまう場合などには、開けるためのスイッチ扉で塞いでしまうのも良いかもしれません。

#### 04
03から

このプログラムだと、一度スイッチを押したら、そのスイッチと連動する扉が全て開いてしまうため、二度とそのスイッチを押す必要がなくなり、あまり効率がよくありません。スイッチを押すことにより閉まる扉も追加し、より複雑な経路を取らせるように改良してください。

### 05
03から(分岐)

このプログラムだと、一度スイッチを押したら、そのスイッチと連動する扉が全て開いてしまうため、二度とそのスイッチを押す必要がなくなり、あまり効率がよくありません。スイッチを押すことにより閉まる扉も追加し、より複雑な経路を取らせるように改良してください。

例えば、スイッチ1を押すにはスイッチ0がONの状態である必要があり、スイッチ2を押すにはスイッチ0がOFF、スイッチ1がONの状態であることが求められるなら、スイッチ1を押した後にもう一度スイッチ0を押しに行く必要があるため、かなり移動距離を増やすことができます。

### 06
無から

この問題に対して、スイッチと扉の種類・位置を最も最短経路が長くなるように焼きなましで決定するプログラムをC++で作成してほしい。
扉は、常に配置できる最大数で設置してほしい。

### 07
06から

では次に、評価値を「最短経路においてスイッチが押される回数(多いほど良い)」に変更したものを実装してください。


### 08
06から

このプログラムに、扉と対応するスイッチのマンハッタン距離も評価値に入れるように変更したものを実装してください。距離を評価値に足すときの重みは、constの変数として持つようにしてください。このプログラムに、扉と対応するスイッチのマンハッタン距離も評価値に入れるように変更したものを実装してください。距離を評価値に足すときの重みは、constの変数として持つようにしてください。 

