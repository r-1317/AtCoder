# ABC459

## B - 459
- 3文字毎かと思ったら違う
- 変換テーブルを作る

## C - Drop Blocks
- 底上げの回数を記録
- 累積和を取るみたいな
- RE
  - 5ケース
- yだけでかいパターンかこれ
- AC

## D - Adjacent Distinct String
- 4色定理みたいな感じかな
- 単純に在庫の多い順に入れていってはどうか
- 優先度付きキュー
- 普通に違う感じかな
- 2つめを候補に入れた
- TLE
  - 11ケース
- 定数倍な気がする
- CPythonならギリ通った
  - heapqはCPythonのほうがいいのかな

## E - Select from Subtrees
- 各頂点に担当のリスがいる
- DPに近い気がする
- TLE
  - 3
- nCrの計算が膨大になる
- 対処法がわからなかった

---

#ABC456 3000位
パフォーマンスは1143、Ratingは15上がって1015

Aは文字列のスライスで対応

Bは3文字ごとにcが+1されるのかと思ったけれど4文字の場合もあった。
手打ちの配列で変換に対応

Cは箱がi個以上のマスの総和を配列で持つ。新たにa個の箱を持つマスに1加算する場合にa+1個のマスの数に加算だけしてもとを消さないことにより成り立つ。
つまり、`counts[towers[x]] += 1`だけして減らす方の操作をしないことで`counts[i]`にi個以上のマスの数を記録できる。
足切りは回数のみ別で記録し、`counts[y+足切り回数]`で適切な値が出力できる。
うまいこと説明しようと試みたけれど変な文章になった。

Dはその時点で在庫が多い文字を末尾へ加えていく
末尾と同じなら2番目に多い文字を加える
文字の在庫はheapqで管理
PyPyではTLE、CPythonなら通った
heapqを使う場合はCPythonのほうが良いのかな

EはnCrの値が膨大になりすぎてTLE
これの対処法は理解できていない
